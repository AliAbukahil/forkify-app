// using uppercase API_URL because this is basically
// a constant that will never change, using uppercase
// for that kind of variable is kind of a common practice
// especially in a configuration file like this

export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';
export const TIMEOUT_SEC = 10;
